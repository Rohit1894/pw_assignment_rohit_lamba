# FULL local/verified dependency set (reproduces the pipeline exactly, incl.
# Whisper). Use this for local development:  pip install -r requirements-local.txt
# The root requirements.txt is the SLIM Streamlit-Cloud deploy set (no Whisper).
openai-whisper==20250625
easyocr==1.7.2
opencv-python==4.13.0.92
moviepy==2.2.1
Pillow==11.3.0
google-genai==2.8.0
numpy==2.4.6
openpyxl==3.1.5
pandas==3.0.3
sarvamai==0.1.28
