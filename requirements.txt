# Streamlit Community Cloud deploy set. Auto-detected by Streamlit Cloud.
#
# torch MUST be the CPU build. `--extra-index-url` alone is NOT enough — pip
# still picks the newer CUDA wheel from PyPI. The explicit `+cpu` local version
# below is only available on the PyTorch CPU index, so it forces the small CPU
# wheels and the free-tier install stops failing.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu
torchvision==0.20.1+cpu

# Whisper omitted (app force-skips it, saves ~500 MB). pandas/openpyxl omitted
# (offline utility only). Full local set: requirements-local.txt.
streamlit
easyocr
opencv-python-headless
moviepy
Pillow
google-genai
numpy<2.3
sarvamai
