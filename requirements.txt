# Pinned to the versions this pipeline was built and verified against, so a fresh
# install reproduces the same behaviour (the OCR/render/codec stack is sensitive to
# minor-version drift). Bump deliberately and re-test, don't float.
openai-whisper==20250625
easyocr==1.7.2
opencv-python==4.13.0.92
moviepy==2.2.1
Pillow==11.3.0
google-genai==2.8.0
numpy==2.4.6
openpyxl==3.1.5
pandas==3.0.3
