# Streamlit Community Cloud deploy set. Auto-detected by Streamlit Cloud.
#
# CPU-only torch: EasyOCR depends on torch. WITHOUT the CPU index line below,
# pip installs the ~2 GB CUDA build and the free-tier install FAILS
# ("Error installing requirements"). This pulls the small CPU wheels instead.
--extra-index-url https://download.pytorch.org/whl/cpu

# Whisper is intentionally omitted (app force-skips it; saves ~500 MB RAM).
# pandas/openpyxl omitted too — only used by the offline rename utility, not the
# video pipeline. Full local set with everything: requirements-local.txt.
# Pins loosened so wheels resolve on Streamlit Cloud's Python (3.12/3.13),
# not your local Python 3.14.
streamlit
torch
torchvision
easyocr
opencv-python-headless
moviepy
Pillow
google-genai
numpy<2.3
sarvamai
